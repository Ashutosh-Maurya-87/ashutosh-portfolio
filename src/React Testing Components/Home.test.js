import Home from '../pages/home/HomeComponent';

describe('testing home component', () => {
    test('should render home component without any errors',() => {
        expect(Home).toBeDefined()
    })
})